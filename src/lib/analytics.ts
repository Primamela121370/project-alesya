const KEY='olesiapro_attribution';const UTMS=['utm_source','utm_medium','utm_campaign','utm_content'] as const;type EventName='page_view'|'contact_whatsapp_click'|'contact_telegram_click'|'contact_max_click'|'contact_phone_click';type Data=Partial<Record<(typeof UTMS)[number],string>>&{referrer?:string};declare global{interface Window{ym?:(id:number,method:'reachGoal',goal:string,params?:Record<string,unknown>)=>void}}
function read():Data{try{return JSON.parse(sessionStorage.getItem(KEY)??'{}') as Data}catch{return {}}}
export function captureAttribution(){const q=new URLSearchParams(location.search),data={...read()};for(const key of UTMS){const value=q.get(key);if(value)data[key]=value.slice(0,200)}if(!data.referrer&&document.referrer)data.referrer=document.referrer.slice(0,500);sessionStorage.setItem(KEY,JSON.stringify(data));return data}
export function trackEvent(name:EventName){const detail={event:name,page_path:location.pathname,...read()};dispatchEvent(new CustomEvent('olesiapro:analytics',{detail}));const raw=(import.meta.env.VITE_YANDEX_METRIKA_ID as string|undefined)?.trim();const id=raw?Number(raw):0;if(id>0&&typeof window.ym==='function')window.ym(id,'reachGoal',name,detail)}
